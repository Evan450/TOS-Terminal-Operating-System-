-- Optional Utilities set manifest — generated by build-disk.lua.
-- Lists the WHOLE set so the installer can show packages that live
-- on a disk which is not currently inserted, and name the disk to
-- ask for. Data only.
return {
  set = "optional-utilities",
  disks = 2,
  packages = {
    ["blockfs"] = { disk = 2, version = "1.0.0", category = "storage", kind = "lib",
      description = "TBFS: a real hierarchical filesystem for unmanaged drives (format/mount/fsck/defrag via the base `drive` command).",
      requires = {  }, recommends = {  } },
    ["calc"] = { disk = 1, version = "1.0.0", category = "productivity", kind = "command",
      description = "Spreadsheet: cells, formulas (SUM/IF/ranges…), save/load, CSV export.",
      requires = {  }, recommends = { "mouse" } },
    ["cluster-manager"] = { disk = 2, version = "1.0.0", category = "network", kind = "service",
      description = "Cluster Manager: registers with Master, accepts assignments, dispatches to workers.",
      requires = {  }, recommends = {  } },
    ["cluster-master"] = { disk = 1, version = "1.0.0", category = "network", kind = "service",
      description = "Cluster control-plane Master: registers Managers, schedules jobs, owns persistent state.",
      requires = {  }, recommends = {  } },
    ["intercom"] = { disk = 2, version = "1.0.0", category = "network", kind = "service",
      description = "Announcement system: plays cataloged tape messages and broadcasts the same words to the network.",
      requires = {  }, recommends = { "tape" } },
    ["mail"] = { disk = 1, version = "1.0.0", category = "network", kind = "service",
      description = "Mesh email: store-and-forward mailbox, end-to-end sealed, with an inbox tab + CLI TUI.",
      requires = {  }, recommends = { "mouse" } },
    ["mouse"] = { disk = 1, version = "1.1.0", category = "drivers", kind = "command",
      description = "Mouse driver: require(\"mouse\") for events + clickable TOS shell UI + `mousetest` demo.",
      requires = {  }, recommends = {  } },
    ["printer"] = { disk = 1, version = "1.0.0", category = "drivers", kind = "command",
      description = "Printer driver for OpenPrinter: require(\"printer\") + a `printer` command (status, print, preview, scan, tags).",
      requires = {  }, recommends = { "mouse" } },
    ["rc-pilot"] = { disk = 2, version = "1.0.0", category = "control", kind = "command",
      description = "WASD remote-control host for OC robots/drones running the rc-pilot EEPROM.",
      requires = {  }, recommends = {  } },
    ["snake"] = { disk = 2, version = "1.0.0", category = "games", kind = "command",
      description = "Classic snake with per-user high scores. Requires a T2+ screen.",
      requires = {  }, recommends = {  } },
    ["stock"] = { disk = 1, version = "1.0.0", category = "automation", kind = "command",
      description = "Inventory monitor: totals every adjacent chest, warns on low stock.",
      requires = {  }, recommends = { "mouse" } },
    ["tape"] = { disk = 2, version = "2.2.1", category = "storage", kind = "command",
      description = "General Computronics tape control: data archive/restore + audio playback + raw I/O + device state.",
      requires = {  }, recommends = {  } },
    ["tape-authenticator"] = { disk = 2, version = "1.0.1", category = "security", kind = "command",
      description = "Tape keycard + encrypted personal log: HMAC identity for access control, private notes you can edit any time.",
      requires = {  }, recommends = { "tape" } },
    ["tetris"] = { disk = 1, version = "1.1.1", category = "games", kind = "command",
      description = "Classic Tetris with per-user high scores. Requires a T2+ screen.",
      requires = {  }, recommends = { "mouse" } },
    ["ttt"] = { disk = 1, version = "1.0.0", category = "games", kind = "command",
      description = "Tic-tac-toe vs an unbeatable AI, or 2-player. Requires a T2+ screen.",
      requires = {  }, recommends = { "mouse" } },
    ["write"] = { disk = 1, version = "1.0.0", category = "productivity", kind = "command",
      description = "Word processor: live page breaks, ink/paper cost, dot-command formatting, prints via the printer driver.",
      requires = { "printer" }, recommends = { "mouse" } },
  },
}
